import 'package:flutter/material.dart';

class GreenPlants extends StatelessWidget {
  const GreenPlants({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
